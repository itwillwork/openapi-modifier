const config = {
    // addAfter: 'after',
    // addBefore: 'before',
    replace: [
        {
            searchValue: '3.0.0',
            replaceValue: '3.0.1'
        }
    ]
};

export default config;