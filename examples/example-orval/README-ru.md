[🇺🇸 English](./README.md) | [🇷🇺 Русский](./README-ru.md)  | [🇨🇳 中文](./README-zh.md)


# Пример: Типизированный Native Fetch с Orval

В этом проекте показано, как генерировать TypeScript-типы и функции API-клиента из спецификации OpenAPI с помощью Orval и использовать их с типобезопасной обёрткой над нативным `fetch`.

## Структура проекта

- `src/` – Исходный код приложения
  - `api/` – Клиент для API и типы
    - `generated/` – Сгенерированные API-клиент и типы от Orval
  - `App.tsx`, `index.tsx` – Точки входа React-приложения
- `specs/` – OpenAPI-спецификации
  - `openapi.json` – Оригинальная спецификация OpenAPI
  - `prepared-openapi.json` – Модифицированная спецификация (после выполнения скриптов)
- `openapi-modifier.config.ts` – Конфиг для модификации OpenAPI-спецификации
- `orval.config.ts` – Конфиг для генерации кода Orval

## Доступные npm-скрипты

- `generate-types` – Генерирует TypeScript-типы и функции API-клиента из подготовленной OpenAPI-спецификации с помощью Orval.
- `prepare-openapi` – Модифицирует OpenAPI-спецификацию с помощью CLI-инструмента `openapi-modifier`.
- `mock:api` – Запускает мок-сервер на основе подготовленной OpenAPI-спецификации.
- `dev` – Одновременно запускает мок-сервер и React-приложение.

## Как это работает

### Генерация типов и клиента из OpenAPI
- Скрипт `prepare-openapi` модифицирует оригинальную спецификацию OpenAPI (например, меняет base path, фильтрует эндпоинты) с помощью `openapi-modifier`.
- Скрипт `generate-types` использует `orval` для генерации TypeScript-типов и функций API-клиента из модифицированной спецификации.
- Orval генерирует типобезопасные функции API, которые используют нативный `fetch` напрямую.

### Типизированный нативный fetch
- Orval генерирует функции API (например, `getPetById`), которые используют нативный `fetch`, обеспечивая полную типобезопасность для запросов и ответов.
- Базовый URL настраивается в `orval.config.ts` и автоматически включается во все API-вызовы.

### Мок-сервер
- Скрипт `mock:api` запускает мок-сервер с помощью `@stoplight/prism-cli` и подготовленной OpenAPI-спецификации для локального тестирования без реального бэкенда.

### Модификация спецификации OpenAPI
- OpenAPI-спецификация модифицируется с помощью набора правил, определённых в `openapi-modifier.config.ts` (например, смена base path, фильтрация эндпоинтов, удаление неиспользуемых компонентов).

