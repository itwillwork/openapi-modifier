merge vs deepmerge

Предположим есть схема:
```js
{
    foo: {
        bar: [
            
        ]
    }
}
```

И вносим изменения в `` следующим diff'ом

enum расширится

объект расширится другими полями